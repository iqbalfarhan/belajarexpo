import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  paragraf: {
    fontSize: 24,
  },
  heading: {
    fontSize: 32,
    fontWeight: 'bold',
  },
  input: {
    borderColor: 'black',
    borderWidth: 1,
    lineHeight: 24,
    padding: 5,
  },
});

export default styles;
